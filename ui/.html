<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>LibraryOS — Management System</title>
<link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;700;900&family=DM+Mono:wght@300;400;500&family=DM+Sans:wght@300;400;500;600&display=swap" rel="stylesheet">
<style>
  :root {
    --bg: #0d0f14;
    --surface: #13161e;
    --surface2: #1a1e2a;
    --surface3: #222636;
    --border: #2a2f42;
    --accent: #c8a96e;
    --accent2: #e8c98a;
    --accent-dim: rgba(200,169,110,0.15);
    --red: #e05555;
    --green: #5ec98a;
    --blue: #5b9cf6;
    --purple: #a78bfa;
    --text: #e8eaf2;
    --text2: #9aa0b8;
    --text3: #5a6080;
    --radius: 10px;
    --shadow: 0 4px 24px rgba(0,0,0,0.5);
  }
  * { margin:0; padding:0; box-sizing:border-box; }
  html, body { height:100%; font-family:'DM Sans', sans-serif; background:var(--bg); color:var(--text); overflow:hidden; }

  /* ── NOISE OVERLAY ── */
  body::before {
    content:''; position:fixed; inset:0; pointer-events:none; z-index:0;
    background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 200 200' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23n)' opacity='0.03'/%3E%3C/svg%3E");
    opacity:0.4;
  }

  /* ── LAYOUT ── */
  #app { display:flex; height:100vh; position:relative; z-index:1; }

  /* ── SIDEBAR ── */
  #sidebar {
    width:220px; min-width:220px; background:var(--surface); border-right:1px solid var(--border);
    display:flex; flex-direction:column; padding:0; overflow:hidden;
    transition: width 0.3s ease;
  }
  .sidebar-logo {
    padding:24px 20px 20px;
    border-bottom:1px solid var(--border);
  }
  .sidebar-logo .logo-text {
    font-family:'Playfair Display', serif;
    font-size:20px; font-weight:900; color:var(--accent);
    letter-spacing:-0.5px;
  }
  .sidebar-logo .logo-sub { font-size:10px; color:var(--text3); font-family:'DM Mono', monospace; letter-spacing:2px; margin-top:2px; }
  .sidebar-user {
    padding:14px 20px; border-bottom:1px solid var(--border);
    display:flex; align-items:center; gap:10px;
  }
  .user-avatar {
    width:34px; height:34px; border-radius:50%; background:var(--accent-dim);
    border:1.5px solid var(--accent); display:flex; align-items:center; justify-content:center;
    font-size:13px; font-weight:700; color:var(--accent); flex-shrink:0;
  }
  .user-info { flex:1; min-width:0; }
  .user-info .uname { font-size:12px; font-weight:600; color:var(--text); white-space:nowrap; overflow:hidden; text-overflow:ellipsis; }
  .user-info .urole { font-size:10px; color:var(--text3); font-family:'DM Mono', monospace; }
  .nav-section { padding:12px 0 4px; }
  .nav-label { font-size:9px; color:var(--text3); font-family:'DM Mono', monospace; letter-spacing:2px; padding:0 20px 6px; }
  .nav-item {
    display:flex; align-items:center; gap:10px; padding:9px 20px; cursor:pointer;
    font-size:13px; color:var(--text2); transition:all 0.15s; border-left:2px solid transparent;
    user-select:none;
  }
  .nav-item:hover { background:var(--surface2); color:var(--text); }
  .nav-item.active { background:var(--accent-dim); color:var(--accent); border-left-color:var(--accent); }
  .nav-item .nav-icon { font-size:15px; width:18px; text-align:center; flex-shrink:0; }
  .sidebar-bottom { margin-top:auto; padding:14px; border-top:1px solid var(--border); }
  .btn-logout {
    width:100%; padding:9px; background:transparent; border:1px solid var(--border);
    color:var(--text2); border-radius:var(--radius); cursor:pointer; font-size:12px;
    font-family:'DM Sans',sans-serif; transition:all 0.15s;
  }
  .btn-logout:hover { background:rgba(224,85,85,0.1); border-color:var(--red); color:var(--red); }

  /* ── MAIN ── */
  #main { flex:1; display:flex; flex-direction:column; overflow:hidden; }
  .topbar {
    padding:16px 28px; border-bottom:1px solid var(--border); background:var(--surface);
    display:flex; align-items:center; justify-content:space-between; flex-shrink:0;
  }
  .topbar-title { font-family:'Playfair Display', serif; font-size:22px; font-weight:700; color:var(--text); }
  .topbar-meta { font-size:11px; color:var(--text3); font-family:'DM Mono',monospace; margin-top:2px; }
  .topbar-actions { display:flex; gap:10px; align-items:center; }
  #content { flex:1; overflow-y:auto; padding:24px 28px; }

  /* ── CARDS & GRIDS ── */
  .stats-grid { display:grid; grid-template-columns:repeat(4,1fr); gap:16px; margin-bottom:24px; }
  .stat-card {
    background:var(--surface); border:1px solid var(--border); border-radius:var(--radius);
    padding:18px 20px; position:relative; overflow:hidden; transition:border-color 0.2s;
  }
  .stat-card:hover { border-color:var(--accent); }
  .stat-card .sc-label { font-size:10px; color:var(--text3); font-family:'DM Mono',monospace; letter-spacing:1.5px; margin-bottom:8px; }
  .stat-card .sc-value { font-family:'Playfair Display',serif; font-size:32px; font-weight:700; color:var(--accent); line-height:1; }
  .stat-card .sc-sub { font-size:11px; color:var(--text3); margin-top:4px; }
  .stat-card .sc-icon { position:absolute; right:16px; top:16px; font-size:24px; opacity:0.15; }

  .section-header { display:flex; align-items:center; justify-content:space-between; margin-bottom:16px; }
  .section-title { font-family:'Playfair Display',serif; font-size:18px; font-weight:700; }
  .section-actions { display:flex; gap:8px; align-items:center; }

  /* ── TABLE ── */
  .table-wrap { background:var(--surface); border:1px solid var(--border); border-radius:var(--radius); overflow:hidden; }
  table { width:100%; border-collapse:collapse; }
  thead th {
    background:var(--surface2); padding:11px 16px; text-align:left;
    font-size:10px; color:var(--text3); font-family:'DM Mono',monospace; letter-spacing:1.5px;
    border-bottom:1px solid var(--border); font-weight:500;
  }
  tbody tr { border-bottom:1px solid var(--border); transition:background 0.15s; }
  tbody tr:last-child { border-bottom:none; }
  tbody tr:hover { background:var(--surface2); }
  tbody td { padding:11px 16px; font-size:13px; color:var(--text2); vertical-align:middle; }
  tbody td.primary { color:var(--text); font-weight:500; }

  /* ── BADGES ── */
  .badge {
    display:inline-flex; align-items:center; padding:3px 9px; border-radius:20px;
    font-size:10px; font-family:'DM Mono',monospace; font-weight:500; letter-spacing:0.5px;
  }
  .badge-green { background:rgba(94,201,138,0.12); color:var(--green); border:1px solid rgba(94,201,138,0.2); }
  .badge-red   { background:rgba(224,85,85,0.12);  color:var(--red);   border:1px solid rgba(224,85,85,0.2);  }
  .badge-gold  { background:rgba(200,169,110,0.12);color:var(--accent);border:1px solid rgba(200,169,110,0.2);}
  .badge-blue  { background:rgba(91,156,246,0.12); color:var(--blue);  border:1px solid rgba(91,156,246,0.2); }
  .badge-purple{ background:rgba(167,139,250,0.12);color:var(--purple);border:1px solid rgba(167,139,250,0.2);}

  /* ── BUTTONS ── */
  .btn {
    padding:8px 16px; border-radius:8px; border:none; cursor:pointer; font-size:12px;
    font-family:'DM Sans',sans-serif; font-weight:500; transition:all 0.15s; display:inline-flex; align-items:center; gap:6px;
  }
  .btn-primary { background:var(--accent); color:#0d0f14; }
  .btn-primary:hover { background:var(--accent2); }
  .btn-secondary { background:var(--surface2); color:var(--text2); border:1px solid var(--border); }
  .btn-secondary:hover { background:var(--surface3); color:var(--text); }
  .btn-danger { background:rgba(224,85,85,0.1); color:var(--red); border:1px solid rgba(224,85,85,0.2); }
  .btn-danger:hover { background:rgba(224,85,85,0.2); }
  .btn-success { background:rgba(94,201,138,0.1); color:var(--green); border:1px solid rgba(94,201,138,0.2); }
  .btn-success:hover { background:rgba(94,201,138,0.2); }
  .btn-sm { padding:5px 10px; font-size:11px; }

  /* ── FORM ── */
  .form-group { margin-bottom:16px; }
  .form-label { display:block; font-size:11px; color:var(--text3); font-family:'DM Mono',monospace; letter-spacing:1px; margin-bottom:6px; }
  .form-control {
    width:100%; padding:10px 14px; background:var(--surface2); border:1px solid var(--border);
    border-radius:8px; color:var(--text); font-size:13px; font-family:'DM Sans',sans-serif;
    outline:none; transition:border-color 0.2s;
  }
  .form-control:focus { border-color:var(--accent); }
  .form-control option { background:var(--surface2); }
  .form-row { display:grid; grid-template-columns:1fr 1fr; gap:14px; }
  .form-actions { display:flex; gap:10px; justify-content:flex-end; margin-top:20px; }

  /* ── MODAL ── */
  .modal-overlay {
    position:fixed; inset:0; background:rgba(0,0,0,0.7); backdrop-filter:blur(4px);
    z-index:1000; display:flex; align-items:center; justify-content:center;
    opacity:0; pointer-events:none; transition:opacity 0.2s;
  }
  .modal-overlay.show { opacity:1; pointer-events:all; }
  .modal {
    background:var(--surface); border:1px solid var(--border); border-radius:14px;
    width:520px; max-width:90vw; max-height:85vh; overflow-y:auto;
    transform:translateY(20px) scale(0.97); transition:transform 0.2s;
    box-shadow:0 24px 80px rgba(0,0,0,0.7);
  }
  .modal-overlay.show .modal { transform:translateY(0) scale(1); }
  .modal-header { padding:20px 24px 16px; border-bottom:1px solid var(--border); display:flex; align-items:center; justify-content:space-between; }
  .modal-title { font-family:'Playfair Display',serif; font-size:18px; font-weight:700; }
  .modal-close { background:none; border:none; color:var(--text3); cursor:pointer; font-size:20px; padding:2px 6px; border-radius:4px; transition:color 0.15s; }
  .modal-close:hover { color:var(--text); }
  .modal-body { padding:20px 24px 24px; }

  /* ── SEARCH & FILTER BAR ── */
  .filter-bar { display:flex; gap:10px; margin-bottom:18px; flex-wrap:wrap; }
  .search-input {
    flex:1; min-width:200px; padding:9px 14px; background:var(--surface2);
    border:1px solid var(--border); border-radius:8px; color:var(--text);
    font-size:13px; font-family:'DM Sans',sans-serif; outline:none;
  }
  .search-input:focus { border-color:var(--accent); }
  .filter-select {
    padding:9px 14px; background:var(--surface2); border:1px solid var(--border);
    border-radius:8px; color:var(--text2); font-size:12px; font-family:'DM Sans',sans-serif;
    outline:none; cursor:pointer;
  }
  .filter-select:focus { border-color:var(--accent); }

  /* ── LOGIN SCREEN ── */
  #login-screen {
    position:fixed; inset:0; z-index:2000;
    background:var(--bg); display:flex; align-items:center; justify-content:center;
  }
  .login-box {
    background:var(--surface); border:1px solid var(--border); border-radius:18px;
    padding:44px 48px; width:420px; box-shadow:var(--shadow);
    animation:fadeUp 0.4s ease forwards;
  }
  @keyframes fadeUp { from{opacity:0;transform:translateY(20px)} to{opacity:1;transform:translateY(0)} }
  .login-logo { font-family:'Playfair Display',serif; font-size:32px; font-weight:900; color:var(--accent); margin-bottom:4px; }
  .login-sub { font-size:11px; color:var(--text3); font-family:'DM Mono',monospace; letter-spacing:2px; margin-bottom:32px; }
  .login-tabs { display:flex; gap:0; margin-bottom:24px; background:var(--surface2); border-radius:8px; padding:3px; }
  .login-tab { flex:1; padding:8px; text-align:center; border-radius:6px; cursor:pointer; font-size:12px; font-weight:500; color:var(--text3); transition:all 0.2s; }
  .login-tab.active { background:var(--accent); color:#0d0f14; }
  .login-error { background:rgba(224,85,85,0.1); border:1px solid rgba(224,85,85,0.2); color:var(--red); padding:10px 14px; border-radius:8px; font-size:12px; margin-bottom:14px; display:none; }
  .login-error.show { display:block; }
  .login-footer { text-align:center; font-size:11px; color:var(--text3); margin-top:20px; }
  .demo-hint { background:var(--surface2); border:1px solid var(--border); border-radius:8px; padding:12px; font-size:11px; color:var(--text3); font-family:'DM Mono',monospace; line-height:1.7; margin-top:16px; }

  /* ── TABS ── */
  .tab-nav { display:flex; gap:0; background:var(--surface2); border-radius:8px; padding:3px; margin-bottom:20px; width:fit-content; }
  .tab-btn { padding:7px 16px; border-radius:6px; border:none; background:transparent; cursor:pointer; font-size:12px; font-weight:500; color:var(--text3); transition:all 0.2s; font-family:'DM Sans',sans-serif; }
  .tab-btn.active { background:var(--surface3); color:var(--text); }

  /* ── TOAST ── */
  #toast-container { position:fixed; bottom:24px; right:24px; z-index:9999; display:flex; flex-direction:column; gap:8px; }
  .toast {
    padding:12px 18px; border-radius:10px; font-size:13px; max-width:320px;
    border:1px solid; backdrop-filter:blur(12px); animation:toastIn 0.3s ease;
    display:flex; align-items:center; gap:8px;
  }
  @keyframes toastIn { from{opacity:0;transform:translateX(40px)} to{opacity:1;transform:translateX(0)} }
  .toast-success { background:rgba(94,201,138,0.12); border-color:rgba(94,201,138,0.25); color:var(--green); }
  .toast-error   { background:rgba(224,85,85,0.12);  border-color:rgba(224,85,85,0.25);  color:var(--red);   }
  .toast-info    { background:rgba(91,156,246,0.12); border-color:rgba(91,156,246,0.25); color:var(--blue);  }

  /* ── PROFILE CARD ── */
  .profile-card {
    background:var(--surface); border:1px solid var(--border); border-radius:var(--radius);
    padding:24px; margin-bottom:20px;
  }
  .profile-header { display:flex; align-items:center; gap:18px; margin-bottom:20px; }
  .profile-avatar {
    width:64px; height:64px; border-radius:50%; background:var(--accent-dim);
    border:2px solid var(--accent); display:flex; align-items:center; justify-content:center;
    font-size:24px; font-weight:700; color:var(--accent); flex-shrink:0;
  }
  .profile-info h2 { font-family:'Playfair Display',serif; font-size:22px; margin-bottom:4px; }
  .profile-meta { font-size:12px; color:var(--text3); font-family:'DM Mono',monospace; }
  .profile-stats { display:grid; grid-template-columns:repeat(3,1fr); gap:12px; }
  .pstat { background:var(--surface2); border:1px solid var(--border); border-radius:8px; padding:14px; text-align:center; }
  .pstat .pstat-val { font-family:'Playfair Display',serif; font-size:24px; font-weight:700; color:var(--accent); }
  .pstat .pstat-label { font-size:10px; color:var(--text3); font-family:'DM Mono',monospace; letter-spacing:1px; margin-top:2px; }

  /* ── MEMBERSHIP CARDS ── */
  .membership-grid { display:grid; grid-template-columns:repeat(3,1fr); gap:16px; margin-bottom:24px; }
  .mem-card {
    background:var(--surface); border:1px solid var(--border); border-radius:var(--radius);
    padding:20px; cursor:pointer; transition:all 0.2s; position:relative; overflow:hidden;
  }
  .mem-card:hover { border-color:var(--accent); transform:translateY(-2px); }
  .mem-card.current { border-color:var(--accent); background:var(--accent-dim); }
  .mem-card .mc-tier { font-size:10px; color:var(--text3); font-family:'DM Mono',monospace; letter-spacing:2px; margin-bottom:8px; }
  .mem-card .mc-name { font-family:'Playfair Display',serif; font-size:20px; font-weight:700; margin-bottom:4px; }
  .mem-card .mc-price { font-size:13px; color:var(--accent); margin-bottom:12px; }
  .mem-card .mc-perks { font-size:11px; color:var(--text3); line-height:1.7; }
  .mem-card .mc-badge { position:absolute; top:12px; right:12px; }

  /* ── HISTORY ── */
  .history-item {
    background:var(--surface2); border:1px solid var(--border); border-radius:8px;
    padding:14px 16px; margin-bottom:10px; display:flex; align-items:center; gap:16px;
  }
  .hi-icon { width:36px; height:36px; border-radius:8px; display:flex; align-items:center; justify-content:center; flex-shrink:0; font-size:16px; }
  .hi-icon.borrowed { background:rgba(91,156,246,0.12); }
  .hi-icon.returned { background:rgba(94,201,138,0.12); }
  .hi-icon.overdue  { background:rgba(224,85,85,0.12); }
  .hi-info { flex:1; min-width:0; }
  .hi-title { font-weight:500; font-size:13px; margin-bottom:3px; white-space:nowrap; overflow:hidden; text-overflow:ellipsis; }
  .hi-meta  { font-size:11px; color:var(--text3); font-family:'DM Mono',monospace; }

  /* ── BOOK CARD GRID ── */
  .books-grid { display:grid; grid-template-columns:repeat(auto-fill,minmax(200px,1fr)); gap:16px; }
  .book-card {
    background:var(--surface); border:1px solid var(--border); border-radius:var(--radius);
    padding:18px; transition:all 0.2s; cursor:default;
  }
  .book-card:hover { border-color:var(--accent); transform:translateY(-2px); box-shadow:0 8px 32px rgba(0,0,0,0.3); }
  .book-type { font-size:9px; font-family:'DM Mono',monospace; letter-spacing:2px; color:var(--text3); margin-bottom:8px; }
  .book-title { font-family:'Playfair Display',serif; font-size:15px; font-weight:700; margin-bottom:4px; line-height:1.3; }
  .book-author { font-size:12px; color:var(--text3); margin-bottom:10px; }
  .book-meta { display:flex; align-items:center; justify-content:space-between; margin-bottom:12px; }
  .book-rating { font-size:12px; color:var(--accent); font-family:'DM Mono',monospace; }
  .book-actions { display:flex; gap:6px; flex-wrap:wrap; }

  /* ── SCROLLBAR ── */
  ::-webkit-scrollbar { width:6px; height:6px; }
  ::-webkit-scrollbar-track { background:var(--surface); }
  ::-webkit-scrollbar-thumb { background:var(--border); border-radius:4px; }
  ::-webkit-scrollbar-thumb:hover { background:var(--text3); }

  /* ── EMPTY STATE ── */
  .empty-state { text-align:center; padding:48px 24px; color:var(--text3); }
  .empty-state .es-icon { font-size:48px; margin-bottom:14px; opacity:0.4; }
  .empty-state .es-text { font-size:14px; margin-bottom:4px; }
  .empty-state .es-sub  { font-size:12px; font-family:'DM Mono',monospace; }

  /* ── FINE INDICATOR ── */
  .fine-bar { background:rgba(224,85,85,0.08); border:1px solid rgba(224,85,85,0.2); border-radius:8px; padding:12px 16px; display:flex; align-items:center; justify-content:space-between; margin-bottom:16px; }
  .fine-bar .fine-label { font-size:12px; color:var(--red); font-family:'DM Mono',monospace; }
  .fine-bar .fine-amount { font-size:18px; font-weight:700; color:var(--red); font-family:'Playfair Display',serif; }

  /* ── REPORT SECTION ── */
  .report-section { background:var(--surface); border:1px solid var(--border); border-radius:var(--radius); margin-bottom:16px; overflow:hidden; }
  .report-header { padding:14px 18px; background:var(--surface2); border-bottom:1px solid var(--border); font-size:12px; font-family:'DM Mono',monospace; color:var(--text3); letter-spacing:1px; display:flex; align-items:center; justify-content:space-between; }
  .report-body { padding:16px 18px; }

  .hidden { display:none !important; }
</style>
</head>
<body>

<!-- LOGIN SCREEN -->
<div id="login-screen">
  <div class="login-box">
    <div class="login-logo">LibraryOS</div>
    <div class="login-sub">MANAGEMENT SYSTEM v2.0</div>
    <div class="login-tabs">
      <div class="login-tab active" onclick="switchLoginTab('user')">User Login</div>
      <div class="login-tab" onclick="switchLoginTab('admin')">Admin Login</div>
      <div class="login-tab" onclick="switchLoginTab('register')">Register</div>
    </div>

    <div id="login-error" class="login-error"></div>

    <!-- User Login -->
    <div id="tab-user">
      <div class="form-group">
        <label class="form-label">EMAIL ADDRESS</label>
        <input class="form-control" id="u-email" type="email" placeholder="user@email.com" value="ali@email.com">
      </div>
      <div class="form-group">
        <label class="form-label">PASSWORD</label>
        <input class="form-control" id="u-pass" type="password" placeholder="••••••••" value="1234">
      </div>
      <button class="btn btn-primary" style="width:100%;justify-content:center;padding:11px;" onclick="doLogin('user')">Sign In</button>
    </div>

    <!-- Admin Login -->
    <div id="tab-admin" class="hidden">
      <div class="form-group">
        <label class="form-label">ADMIN EMAIL</label>
        <input class="form-control" id="a-email" type="email" placeholder="admin@library.com" value="admin@library.com">
      </div>
      <div class="form-group">
        <label class="form-label">PASSWORD</label>
        <input class="form-control" id="a-pass" type="password" placeholder="••••••••" value="adminpass">
      </div>
      <button class="btn btn-primary" style="width:100%;justify-content:center;padding:11px;" onclick="doLogin('admin')">Admin Sign In</button>
    </div>

    <!-- Register -->
    <div id="tab-register" class="hidden">
      <div class="form-row">
        <div class="form-group">
          <label class="form-label">FIRST NAME</label>
          <input class="form-control" id="r-fname" placeholder="First Name">
        </div>
        <div class="form-group">
          <label class="form-label">LAST NAME</label>
          <input class="form-control" id="r-lname" placeholder="Last Name">
        </div>
      </div>
      <div class="form-group">
        <label class="form-label">EMAIL ADDRESS</label>
        <input class="form-control" id="r-email" type="email" placeholder="your@email.com">
      </div>
      <div class="form-group">
        <label class="form-label">PASSWORD</label>
        <input class="form-control" id="r-pass" type="password" placeholder="Create password">
      </div>
      <div class="form-group">
        <label class="form-label">INITIAL BALANCE (Rs.)</label>
        <input class="form-control" id="r-balance" type="number" placeholder="0.00" value="100">
      </div>
      <button class="btn btn-primary" style="width:100%;justify-content:center;padding:11px;" onclick="doRegister()">Create Account</button>
    </div>

    <div class="demo-hint">
      <strong>Demo Credentials:</strong><br>
      User: ali@email.com / 1234<br>
      Admin: admin@library.com / adminpass
    </div>
  </div>
</div>

<!-- MAIN APP -->
<div id="app" class="hidden">
  <!-- SIDEBAR -->
  <div id="sidebar">
    <div class="sidebar-logo">
      <div class="logo-text">LibraryOS</div>
      <div class="logo-sub">MGMT SYSTEM</div>
    </div>
    <div class="sidebar-user">
      <div class="user-avatar" id="sb-avatar">U</div>
      <div class="user-info">
        <div class="uname" id="sb-name">User</div>
        <div class="urole" id="sb-role">member</div>
      </div>
    </div>

    <!-- User nav -->
    <div id="user-nav">
      <div class="nav-section">
        <div class="nav-label">OVERVIEW</div>
        <div class="nav-item active" onclick="navigate('dashboard')"><span class="nav-icon">🏛</span>Dashboard</div>
      </div>
      <div class="nav-section">
        <div class="nav-label">LIBRARY</div>
        <div class="nav-item" onclick="navigate('browse')"><span class="nav-icon">📚</span>Browse Books</div>
        <div class="nav-item" onclick="navigate('my-books')"><span class="nav-icon">📖</span>My Books</div>
        <div class="nav-item" onclick="navigate('history')"><span class="nav-icon">🕑</span>History</div>
      </div>
      <div class="nav-section">
        <div class="nav-label">ACCOUNT</div>
        <div class="nav-item" onclick="navigate('profile')"><span class="nav-icon">👤</span>Profile</div>
        <div class="nav-item" onclick="navigate('membership')"><span class="nav-icon">⭐</span>Membership</div>
        <div class="nav-item" onclick="navigate('loyalty')"><span class="nav-icon">🎁</span>Loyalty Points</div>
        <div class="nav-item" onclick="navigate('wallet')"><span class="nav-icon">💰</span>Wallet</div>
      </div>
    </div>

    <!-- Admin nav -->
    <div id="admin-nav" class="hidden">
      <div class="nav-section">
        <div class="nav-label">ADMIN PANEL</div>
        <div class="nav-item active" onclick="navigate('admin-dashboard')"><span class="nav-icon">📊</span>Dashboard</div>
        <div class="nav-item" onclick="navigate('manage-books')"><span class="nav-icon">📚</span>Manage Books</div>
        <div class="nav-item" onclick="navigate('manage-users')"><span class="nav-icon">👥</span>Manage Users</div>
        <div class="nav-item" onclick="navigate('reports')"><span class="nav-icon">📋</span>Reports</div>
      </div>
    </div>

    <div class="sidebar-bottom">
      <button class="btn-logout" onclick="doLogout()">⬡ Sign Out</button>
    </div>
  </div>

  <!-- MAIN CONTENT -->
  <div id="main">
    <div class="topbar">
      <div>
        <div class="topbar-title" id="page-title">Dashboard</div>
        <div class="topbar-meta" id="page-meta">Overview of your library activity</div>
      </div>
      <div class="topbar-actions" id="topbar-actions"></div>
    </div>
    <div id="content"></div>
  </div>
</div>

<!-- MODAL -->
<div class="modal-overlay" id="modal-overlay" onclick="closeModalOutside(event)">
  <div class="modal" id="modal">
    <div class="modal-header">
      <div class="modal-title" id="modal-title">Modal</div>
      <button class="modal-close" onclick="closeModal()">✕</button>
    </div>
    <div class="modal-body" id="modal-body"></div>
  </div>
</div>

<!-- TOAST -->
<div id="toast-container"></div>

<script>
// ═══════════════════════════════════════════════════════
//  DATA STORE — All data persisted in localStorage as JSON
// ═══════════════════════════════════════════════════════

const LS_KEY = 'libraryos_data';

function getDefaultData() {
  return {
    users: [
      { id:1, firstName:'Ali',    lastName:'Khan',    email:'ali@email.com',     password:'1234',      balance:500, loyaltyPoints:0,  membership:'Deluxe',    isLocked:false, borrowHistory:[], currentlyBorrowed:[] },
      { id:2, firstName:'Sara',   lastName:'Ahmed',   email:'sara@gmail.com',    password:'5678',      balance:350, loyaltyPoints:0,  membership:'Extra',     isLocked:false, borrowHistory:[], currentlyBorrowed:[] },
      { id:3, firstName:'Hassan', lastName:'Mohamed', email:'hassan@email.com',  password:'9999',      balance:200, loyaltyPoints:0,  membership:'Essential', isLocked:false, borrowHistory:[], currentlyBorrowed:[] },
      { id:4, firstName:'Fatima', lastName:'Ali',     email:'fatima@email.com',  password:'1111',      balance:100, loyaltyPoints:0,  membership:'Essential', isLocked:false, borrowHistory:[], currentlyBorrowed:[] },
    ],
    admins: [
      { id:1, firstName:'Ahmad', lastName:'Raza', email:'admin@library.com', password:'adminpass', accessLevel:'SuperAdmin' }
    ],
    resources: [
      { id:1, type:'PrimePick',    title:'The Great Gatsby',         author:'F. Scott Fitzgerald', category:'Fiction',    available:true, rating:0, borrowCount:0, isNewArrival:true },
      { id:2, type:'ClassicShelf', title:'To Kill a Mockingbird',    author:'Harper Lee',          category:'Fiction',    available:true, rating:0, borrowCount:0, isNewArrival:true },
      { id:3, type:'BudgetPick',   title:'1984',                     author:'George Orwell',       category:'Fiction',    available:true, rating:0, borrowCount:0, isNewArrival:true },
      { id:4, type:'PrimePick',    title:'A Brief History of Time',  author:'Stephen Hawking',     category:'Science',    available:true, rating:0, borrowCount:0, isNewArrival:true },
      { id:5, type:'ClassicShelf', title:'Sapiens',                  author:'Yuval Noah Harari',   category:'History',    available:true, rating:0, borrowCount:0, isNewArrival:true },
      { id:6, type:'BudgetPick',   title:'Clean Code',               author:'Robert C. Martin',    category:'Technology', available:true, rating:0, borrowCount:0, isNewArrival:true },
    ],
    nextUserId: 5,
    nextResourceId: 7,
  };
}

function loadData() {
  try {
    const raw = localStorage.getItem(LS_KEY);
    if (!raw) return getDefaultData();
    return JSON.parse(raw);
  } catch { return getDefaultData(); }
}

function saveData() {
  localStorage.setItem(LS_KEY, JSON.stringify(db));
}

let db = loadData();

// ═══════════════════════════════════════════════════════
//  SESSION
// ═══════════════════════════════════════════════════════

let session = { type: null, id: null }; // type: 'user'|'admin'
let currentPage = '';

function getUser(id) { return db.users.find(u => u.id === id); }
function getAdmin(id) { return db.admins.find(a => a.id === id); }
function getResource(id) { return db.resources.find(r => r.id === id); }
function getCurrentUser() { return getUser(session.id); }
function getCurrentAdmin() { return getAdmin(session.id); }

// ═══════════════════════════════════════════════════════
//  MEMBERSHIP CONFIG
// ═══════════════════════════════════════════════════════

const MEMBERSHIPS = {
  Essential: { name:'Essential', maxBorrow:2, fineDiscount:0.0,  price:0,  fineRate:{PrimePick:5,ClassicShelf:3,BudgetPick:2} },
  Extra:     { name:'Extra',     maxBorrow:5, fineDiscount:0.25, price:10, fineRate:{PrimePick:5,ClassicShelf:3,BudgetPick:2} },
  Deluxe:    { name:'Deluxe',    maxBorrow:10,fineDiscount:0.50, price:20, fineRate:{PrimePick:5,ClassicShelf:3,BudgetPick:2} },
};

function getMembership(name) { return MEMBERSHIPS[name] || MEMBERSHIPS.Essential; }

function getFineRate(type) {
  if (type==='PrimePick') return 5;
  if (type==='ClassicShelf') return 3;
  return 2;
}

function getBorrowDuration() { return 7; }

// ═══════════════════════════════════════════════════════
//  DATE HELPERS
// ═══════════════════════════════════════════════════════

function today() {
  const d = new Date(); 
  return d.toISOString().split('T')[0];
}

function addDays(dateStr, days) {
  const d = new Date(dateStr);
  d.setDate(d.getDate() + days);
  return d.toISOString().split('T')[0];
}

function isOverdue(dueDate, returnDate) {
  const due = new Date(dueDate);
  const ret = new Date(returnDate || today());
  return ret > due;
}

function daysDiff(date1, date2) {
  const d1 = new Date(date1), d2 = new Date(date2);
  return Math.max(0, Math.floor((d2 - d1) / 86400000));
}

function fmtDate(d) {
  if (!d) return '—';
  const parts = d.split('-');
  const months = ['Jan','Feb','Mar','Apr','May','Jun','Jul','Aug','Sep','Oct','Nov','Dec'];
  return `${parts[2]} ${months[parseInt(parts[1])-1]} ${parts[0]}`;
}

// ═══════════════════════════════════════════════════════
//  TOAST
// ═══════════════════════════════════════════════════════

function toast(msg, type='info') {
  const container = document.getElementById('toast-container');
  const el = document.createElement('div');
  const icons = {success:'✓', error:'✕', info:'ℹ'};
  el.className = `toast toast-${type}`;
  el.innerHTML = `<span>${icons[type]||'ℹ'}</span><span>${msg}</span>`;
  container.appendChild(el);
  setTimeout(() => { el.style.opacity='0'; el.style.transform='translateX(40px)'; el.style.transition='all 0.3s'; setTimeout(()=>el.remove(),300); }, 3000);
}

// ═══════════════════════════════════════════════════════
//  LOGIN / LOGOUT
// ═══════════════════════════════════════════════════════

function switchLoginTab(tab) {
  ['user','admin','register'].forEach(t => {
    document.getElementById(`tab-${t}`).classList.toggle('hidden', t!==tab);
    document.querySelectorAll('.login-tab')[['user','admin','register'].indexOf(t)].classList.toggle('active', t===tab);
  });
  document.getElementById('login-error').classList.remove('show');
}

function showLoginError(msg) {
  const el = document.getElementById('login-error');
  el.textContent = msg; el.classList.add('show');
}

function doLogin(type) {
  const email = document.getElementById(`${type[0]}-email`).value.trim();
  const pass  = document.getElementById(`${type[0]}-pass`).value.trim();
  if (!email || !pass) { showLoginError('Please enter email and password.'); return; }

  if (type === 'user') {
    const u = authenticate(email, pass);
    if (!u || !db.users.includes(u)) { showLoginError('Invalid email or password.'); return; }
    if (u.isLocked) { showLoginError('Account is locked. Contact admin.'); return; }
    session = { type:'user', id:u.id };
    enterApp();
  } else {
    const a = authenticate(email, pass);
    if (!a || !db.admins.includes(a)) { showLoginError('Invalid admin credentials.'); return; }
    session = { type:'admin', id:a.id };
    enterApp();
  }
}

function doRegister() {
  const fn = document.getElementById('r-fname').value.trim();
  const ln = document.getElementById('r-lname').value.trim();
  const em = document.getElementById('r-email').value.trim();
  const pw = document.getElementById('r-pass').value.trim();
  const bal= parseFloat(document.getElementById('r-balance').value) || 0;
  if (!fn||!ln||!em||!pw) { showLoginError('Please fill all fields.'); return; }
  if (db.users.find(u=>u.email===em)) { showLoginError('Email already registered.'); return; }
  const nu = registerUser(fn, ln, em, pw, bal);
  session = { type:'user', id:nu.id };
  enterApp();
}

function enterApp() {
  document.getElementById('login-screen').classList.add('hidden');
  document.getElementById('app').classList.remove('hidden');
  updateSidebar();
  if (session.type === 'admin') {
    document.getElementById('user-nav').classList.add('hidden');
    document.getElementById('admin-nav').classList.remove('hidden');
    navigate('admin-dashboard');
  } else {
    document.getElementById('user-nav').classList.remove('hidden');
    document.getElementById('admin-nav').classList.add('hidden');
    navigate('dashboard');
  }
}

function doLogout() {
  session = { type:null, id:null };
  document.getElementById('app').classList.add('hidden');
  document.getElementById('login-screen').classList.remove('hidden');
  toast('Signed out successfully.', 'info');
}

function updateSidebar() {
  if (session.type === 'user') {
    const u = getCurrentUser();
    document.getElementById('sb-name').textContent = u.firstName + ' ' + u.lastName;
    document.getElementById('sb-role').textContent = u.membership.toLowerCase() + ' member';
    document.getElementById('sb-avatar').textContent = u.firstName[0];
  } else {
    const a = getCurrentAdmin();
    document.getElementById('sb-name').textContent = a.firstName + ' ' + a.lastName;
    document.getElementById('sb-role').textContent = a.accessLevel.toLowerCase();
    document.getElementById('sb-avatar').textContent = a.firstName[0];
  }
}

// ═══════════════════════════════════════════════════════
//  NAVIGATION
// ═══════════════════════════════════════════════════════

const pageConfig = {
  'dashboard':       { title:'Dashboard',        meta:'Your library activity at a glance' },
  'browse':          { title:'Browse Books',      meta:'Explore the full library collection' },
  'my-books':        { title:'My Books',          meta:'Books you currently have borrowed' },
  'history':         { title:'Borrow History',    meta:'Your complete borrowing record' },
  'profile':         { title:'My Profile',        meta:'Account details & settings' },
  'membership':      { title:'Membership',        meta:'Manage your membership tier' },
  'loyalty':         { title:'Loyalty Points',    meta:'Earn & redeem your reward points' },
  'wallet':          { title:'Wallet',            meta:'Balance & transactions' },
  'admin-dashboard': { title:'Admin Dashboard',   meta:'System-wide overview' },
  'manage-books':    { title:'Manage Books',      meta:'Add, edit & remove library resources' },
  'manage-users':    { title:'Manage Users',      meta:'User accounts & access control' },
  'reports':         { title:'Reports',           meta:'Detailed system reports' },
};

function navigate(page) {
  currentPage = page;
  document.querySelectorAll('.nav-item').forEach(n => n.classList.toggle('active', n.getAttribute('onclick')===`navigate('${page}')`));
  const cfg = pageConfig[page] || { title:page, meta:'' };
  document.getElementById('page-title').textContent = cfg.title;
  document.getElementById('page-meta').textContent  = cfg.meta;

  const renders = {
    'dashboard':       renderDashboard,
    'browse':          renderBrowse,
    'my-books':        renderMyBooks,
    'history':         renderHistory,
    'profile':         renderProfile,
    'membership':      renderMembership,
    'loyalty':         renderLoyalty,
    'wallet':          renderWallet,
    'admin-dashboard': renderAdminDashboard,
    'manage-books':    renderManageBooks,
    'manage-users':    renderManageUsers,
    'reports':         renderReports,
  };
  const fn = renders[page];
  if (fn) fn();
}

function setContent(html) {
  document.getElementById('content').innerHTML = html;
}

function setTopbarActions(html) {
  document.getElementById('topbar-actions').innerHTML = html;
}

// ═══════════════════════════════════════════════════════
//  BORROW / RETURN LOGIC
// ═══════════════════════════════════════════════════════

function borrowBook(resourceId) {
  const u = getCurrentUser();
  const r = getResource(resourceId);
  if (!r || !r.available) { toast('Book not available.', 'error'); return; }
  if (u.isLocked) { toast('Account locked. Cannot borrow.', 'error'); return; }
  if (u.balance < 0) { toast('Negative balance. Please recharge first.', 'error'); return; }

  const mem = getMembership(u.membership);
  if (u.currentlyBorrowed.length >= mem.maxBorrow) {
    toast(`Borrow limit reached (max ${mem.maxBorrow} for ${u.membership}).`, 'error'); return;
  }

  const borrowDate = today();
  const dueDate = addDays(borrowDate, 7);
  const record = { resourceId:r.id, title:r.title, author:r.author, type:r.type, category:r.category, borrowDate, dueDate, returned:false, returnDate:null };

  u.currentlyBorrowed.push(resourceId);
  u.borrowHistory.push(record);
  u.loyaltyPoints += 5;
  r.available = false;
  r.borrowCount++;
  r.isNewArrival = false;

  saveData();
  toast(`Borrowed "${r.title}" — due ${fmtDate(dueDate)}`, 'success');
  navigate(currentPage);
}

function returnBook(resourceId) {
  const u = getCurrentUser();
  const r = getResource(resourceId);
  if (!r) { toast('Book not found.', 'error'); return; }

  const retDate = today();
  const rec = u.borrowHistory.slice().reverse().find(h => h.resourceId === resourceId && !h.returned);
  if (!rec) { toast('No active borrow record found.', 'error'); return; }

  rec.returned = true;
  rec.returnDate = retDate;
  u.currentlyBorrowed = u.currentlyBorrowed.filter(id => id !== resourceId);
  r.available = true;

  let fine = 0;
  if (isOverdue(rec.dueDate, retDate)) {
    const days = daysDiff(rec.dueDate, retDate);
    const rate = getFineRate(r.type);
    const mem = getMembership(u.membership);
    fine = days * rate * (1 - mem.fineDiscount);
    u.balance -= fine;
    toast(`Returned late! Fine: Rs.${fine.toFixed(2)} (${days} day${days>1?'s':''})`, 'error');
  } else {
    u.loyaltyPoints += 10;
    toast(`Returned on time! +10 loyalty points.`, 'success');
  }

  saveData();
  navigate(currentPage);
}

// ═══════════════════════════════════════════════════════
//  MODAL
// ═══════════════════════════════════════════════════════

function openModal(title, bodyHTML) {
  document.getElementById('modal-title').textContent = title;
  document.getElementById('modal-body').innerHTML = bodyHTML;
  document.getElementById('modal-overlay').classList.add('show');
}
function closeModal() { document.getElementById('modal-overlay').classList.remove('show'); }
function closeModalOutside(e) { if (e.target===document.getElementById('modal-overlay')) closeModal(); }

// ═══════════════════════════════════════════════════════
//  PAGES — USER
// ═══════════════════════════════════════════════════════

function renderDashboard() {
  const u = getCurrentUser();
  const mem = getMembership(u.membership);
  const available = db.resources.filter(r=>r.available).length;
  const borrowed = u.currentlyBorrowed.length;
  const overdueCount = u.borrowHistory.filter(h=>!h.returned && isOverdue(h.dueDate)).length;
  const memBadgeClass = {Essential:'badge-blue', Extra:'badge-gold', Deluxe:'badge-purple'}[u.membership]||'badge-blue';
  const topBooks = db.resources.filter(r=>r.borrowCount>0).sort((a,b)=>b.borrowCount-a.borrowCount).slice(0,3);
  const recentHistory = [...u.borrowHistory].reverse().slice(0,5);

  setTopbarActions('');
  setContent(`
    <div class="stats-grid">
      <div class="stat-card">
        <div class="sc-label">BOOKS BORROWED</div>
        <div class="sc-value">${borrowed}</div>
        <div class="sc-sub">of ${mem.maxBorrow} limit</div>
        <div class="sc-icon">📖</div>
      </div>
      <div class="stat-card">
        <div class="sc-label">ACCOUNT BALANCE</div>
        <div class="sc-value" style="font-size:24px;">Rs.${u.balance.toFixed(0)}</div>
        <div class="sc-sub">${u.balance<0?'<span style="color:var(--red)">Negative balance</span>':'Available'}</div>
        <div class="sc-icon">💰</div>
      </div>
      <div class="stat-card">
        <div class="sc-label">LOYALTY POINTS</div>
        <div class="sc-value">${u.loyaltyPoints}</div>
        <div class="sc-sub">Earn 5 per borrow</div>
        <div class="sc-icon">⭐</div>
      </div>
      <div class="stat-card">
        <div class="sc-label">OVERDUE BOOKS</div>
        <div class="sc-value" style="color:${overdueCount>0?'var(--red)':'var(--green)'}">${overdueCount}</div>
        <div class="sc-sub">${overdueCount>0?'Action required':'All good!'}</div>
        <div class="sc-icon">⏰</div>
      </div>
    </div>

    ${overdueCount>0?`<div class="fine-bar"><div class="fine-label">⚠ OVERDUE BOOKS — FINES ACCUMULATING</div><button class="btn btn-danger btn-sm" onclick="navigate('my-books')">View Overdue</button></div>`:''}

    <div style="display:grid;grid-template-columns:1fr 1fr;gap:20px">
      <div>
        <div class="section-header">
          <div class="section-title">Recent Activity</div>
          <button class="btn btn-secondary btn-sm" onclick="navigate('history')">View All</button>
        </div>
        ${recentHistory.length===0?`<div class="empty-state"><div class="es-icon">📭</div><div class="es-text">No activity yet</div><div class="es-sub">Browse books to get started</div></div>`
        :recentHistory.map(h=>{
          const over = !h.returned && isOverdue(h.dueDate);
          return `<div class="history-item">
            <div class="hi-icon ${h.returned?'returned':over?'overdue':'borrowed'}">${h.returned?'✓':over?'⚠':'📖'}</div>
            <div class="hi-info">
              <div class="hi-title">${h.title}</div>
              <div class="hi-meta">${h.returned?'Returned '+fmtDate(h.returnDate):over?'OVERDUE — Due '+fmtDate(h.dueDate):'Due '+fmtDate(h.dueDate)}</div>
            </div>
            ${over?`<span class="badge badge-red">OVERDUE</span>`:h.returned?`<span class="badge badge-green">RETURNED</span>`:`<span class="badge badge-blue">ACTIVE</span>`}
          </div>`;
        }).join('')}
      </div>
      <div>
        <div class="section-header"><div class="section-title">Membership Status</div></div>
        <div class="profile-card" style="margin-bottom:16px">
          <div style="display:flex;align-items:center;justify-content:space-between;margin-bottom:12px">
            <div>
              <div style="font-family:'Playfair Display',serif;font-size:20px;font-weight:700">${u.membership}</div>
              <div style="font-size:11px;color:var(--text3);font-family:'DM Mono',monospace">${mem.price===0?'FREE':'Rs.'+mem.price+'/month'}</div>
            </div>
            <span class="badge ${memBadgeClass}">${u.membership.toUpperCase()}</span>
          </div>
          <div style="font-size:12px;color:var(--text3);line-height:1.8">
            📚 Borrow up to <strong style="color:var(--text)">${mem.maxBorrow}</strong> books<br>
            💸 <strong style="color:var(--text)">${mem.fineDiscount*100}%</strong> discount on fines<br>
          </div>
          <button class="btn btn-secondary btn-sm" style="margin-top:12px;width:100%;justify-content:center" onclick="navigate('membership')">Manage Membership</button>
        </div>
        <div class="section-header"><div class="section-title">Popular Now</div></div>
        ${topBooks.map((b,i)=>`
          <div class="history-item">
            <div class="hi-icon borrowed" style="font-size:14px;font-weight:700;color:var(--accent)">#${i+1}</div>
            <div class="hi-info">
              <div class="hi-title">${b.title}</div>
              <div class="hi-meta">${b.author} · ${b.borrowCount} borrows</div>
            </div>
            <div style="font-size:11px;color:var(--accent);font-family:'DM Mono',monospace">★${b.rating>0?b.rating.toFixed(1):'—'}</div>
          </div>
        `).join('')}
      </div>
    </div>
  `);
}

function renderBrowse() {
  setTopbarActions(`<input class="search-input" id="browse-search" placeholder="Search books..." oninput="filterBrowse()" style="width:220px">
    <select class="filter-select" id="browse-cat" onchange="filterBrowse()">
      <option value="">All Categories</option>
      <option value="Fiction">Fiction</option>
      <option value="Science">Science</option>
      <option value="History">History</option>
      <option value="Technology">Technology</option>
    </select>
    <select class="filter-select" id="browse-avail" onchange="filterBrowse()">
      <option value="">All</option>
      <option value="available">Available</option>
      <option value="borrowed">Borrowed</option>
    </select>`);

  renderBrowseBooks(db.resources);
}

function filterBrowse() {
  const q = (document.getElementById('browse-search')?.value||'').toLowerCase();
  const cat = document.getElementById('browse-cat')?.value||'';
  const avail = document.getElementById('browse-avail')?.value||'';
  let res = q ? searchResource(q) : db.resources;
  if (cat) res = res.filter(r=>r.category===cat);
  if (avail==='available') res = res.filter(r=>r.available);
  if (avail==='borrowed') res = res.filter(r=>!r.available);
  renderBrowseBooks(res);
}

function renderBrowseBooks(resources) {
  const u = getCurrentUser();
  const typeBadge = {PrimePick:'PRIME PICK',ClassicShelf:'CLASSIC SHELF',BudgetPick:'BUDGET PICK'};
  const typeColor = {PrimePick:'badge-gold',ClassicShelf:'badge-purple',BudgetPick:'badge-green'};

  const html = `
    <div class="books-grid" id="books-grid">
      ${resources.length===0?`<div class="empty-state" style="grid-column:1/-1"><div class="es-icon">🔍</div><div class="es-text">No books found</div><div class="es-sub">Try different filters</div></div>`
      :resources.map(r=>{
        const isBorrowed = u.currentlyBorrowed.includes(r.id);
        return `<div class="book-card">
          <div class="book-type"><span class="badge ${typeColor[r.type]||'badge-blue'}">${typeBadge[r.type]||r.type}</span></div>
          <div class="book-title">${r.title}</div>
          <div class="book-author">${r.author}</div>
          <div class="book-meta">
            <span class="badge ${r.available?'badge-green':'badge-red'}">${r.available?'AVAILABLE':'BORROWED'}</span>
            <div class="book-rating">★ ${r.rating>0?r.rating.toFixed(1):'—'}</div>
          </div>
          <div style="font-size:11px;color:var(--text3);margin-bottom:10px;font-family:'DM Mono',monospace">${r.category} · ${r.borrowCount} borrows</div>
          <div class="book-actions">
            ${r.available && !isBorrowed?`<button class="btn btn-primary btn-sm" onclick="borrowBook(${r.id})">Borrow</button>`:''}
            ${isBorrowed?`<button class="btn btn-success btn-sm" onclick="returnBook(${r.id})">Return</button>`:''}
            <button class="btn btn-secondary btn-sm" onclick="rateBook(${r.id})">Rate ★</button>
          </div>
        </div>`;
      }).join('')}
    </div>`;
  setContent(html);
}

function rateBook(id) {
  const r = getResource(id);
  openModal(`Rate "${r.title}"`, `
    <div style="text-align:center;padding:10px 0">
      <div style="font-size:13px;color:var(--text3);margin-bottom:20px">How would you rate this book?</div>
      <div style="display:flex;gap:12px;justify-content:center;margin-bottom:24px" id="star-row">
        ${[1,2,3,4,5].map(i=>`<span style="font-size:36px;cursor:pointer;opacity:0.4;transition:all 0.1s" onclick="submitRating(${id},${i})" onmouseover="hoverStar(${i})" onmouseout="resetStars(${r.rating})" id="star-${i}">★</span>`).join('')}
      </div>
      <div style="font-size:11px;color:var(--text3);font-family:'DM Mono',monospace">Current rating: ${r.rating>0?r.rating.toFixed(1)+'/5':'No ratings yet'}</div>
    </div>
  `);
  if (r.rating > 0) resetStars(r.rating);
}

function hoverStar(n) { for(let i=1;i<=5;i++){const s=document.getElementById(`star-${i}`); if(s) s.style.opacity=i<=n?'1':'0.4';} }
function resetStars(rating) { for(let i=1;i<=5;i++){const s=document.getElementById(`star-${i}`); if(s) s.style.opacity=i<=rating?'1':'0.4';} }

function submitRating(id, val) {
  const r = getResource(id);
  r.rating = r.rating===0 ? val : (r.rating + val) / 2;
  saveData();
  closeModal();
  toast(`Rated "${r.title}" — ${val} stars!`, 'success');
  navigate(currentPage);
}
function renderReviews(c) {
  const u = state.currentUser;
  const reviewable = state.resources.filter(r=>u.history.some(h=>h.resourceID===r.id));
  c.innerHTML = `
    <div id="review-alert"></div>
    ${reviewable.length===0
      ? '<div class="empty-state"><div class="empty-icon">✦</div>Borrow a book first to leave a review.</div>'
      : `<div style="margin-bottom:18px;color:var(--text2);font-size:13px">You can review any book you have borrowed.</div>
         <div style="display:grid;grid-template-columns:repeat(auto-fill,minmax(260px,1fr));gap:14px">
           ${reviewable.map(r=>`
             <div class="report-card" style="padding:16px">
               <div style="font-weight:500;margin-bottom:2px">${r.title}</div>
               <div style="font-size:12px;color:var(--text3);margin-bottom:10px">${r.author} · ${r.category}</div>
               <div style="display:flex;align-items:center;gap:8px;margin-bottom:8px">
                 ${r.rating>0?`${starsHTML(r.rating)}<span style="font-size:11px;color:var(--text3)">${r.rating.toFixed(1)}/5</span>`:'<span style="font-size:12px;color:var(--text3)">No reviews yet</span>'}
               </div>
               <button class="btn btn-primary btn-sm" onclick="openReviewModal(${r.id})">Write Review</button>
             </div>`).join('')}
         </div>`
    }`;
}
 
function openReviewModal(rid) {
  const r = getResource(rid);
  currentRating = 0;
  openModal(`<div class="modal">
    <h3>Review: "${r.title}"</h3>
    <div class="form-group">
      <label>Your Rating</label>
      <input type="hidden" id="review-stars" value="0">
      <div class="rating-input" id="star-row">
        ${[1,2,3,4,5].map(i=>`<span class="star" onclick="setRating(${i},'review-stars')" style="font-size:26px;cursor:pointer">★</span>`).join('')}
      </div>
    </div>
    <div class="form-group">
      <label>Your Review</label>
      <textarea id="review-text" placeholder="Share your thoughts about this book..." rows="4"></textarea>
    </div>
    <div id="review-modal-alert"></div>
    <div class="modal-actions">
      <button class="btn btn-secondary" onclick="closeModal()">Cancel</button>
      <button class="btn btn-primary" onclick="submitReviewUI(${rid})">Submit Review</button>
    </div>
  </div>`);
}
 
function submitReviewUI(rid) {
  const stars = parseInt(el('review-stars').value)||0;
  const text  = el('review-text').value.trim();
  if (stars===0) { showAlert('review-modal-alert','Please select a star rating.'); return; }
  if (!text)     { showAlert('review-modal-alert','Please write a review.'); return; }
  const result = submitReview(state.currentUser, rid, stars, text);
  if (result.ok) { closeModal(); showAlert('review-alert', result.msg, 'success'); navigate('reviews'); }
  else showAlert('review-modal-alert', result.msg);
}
function renderMyBooks() {
  const u = getCurrentUser();
  setTopbarActions('');

  const activeBorrows = u.borrowHistory.filter(h => !h.returned && u.currentlyBorrowed.includes(h.resourceId));

  setContent(activeBorrows.length === 0
    ? `<div class="empty-state"><div class="es-icon">📚</div><div class="es-text">No books currently borrowed</div><div class="es-sub">Browse the catalog to borrow books</div><button class="btn btn-primary" style="margin-top:16px" onclick="navigate('browse')">Browse Books</button></div>`
    : `
      <div class="table-wrap">
        <table>
          <thead><tr><th>BOOK</th><th>AUTHOR</th><th>CATEGORY</th><th>BORROW DATE</th><th>DUE DATE</th><th>STATUS</th><th>ACTIONS</th></tr></thead>
          <tbody>
            ${activeBorrows.map(h => {
              const over = isOverdue(h.dueDate);
              return `<tr>
                <td class="primary">${h.title}</td>
                <td>${h.author}</td>
                <td><span class="badge badge-blue">${h.category}</span></td>
                <td style="font-family:'DM Mono',monospace;font-size:11px">${fmtDate(h.borrowDate)}</td>
                <td style="font-family:'DM Mono',monospace;font-size:11px">${fmtDate(h.dueDate)}</td>
                <td>${over?`<span class="badge badge-red">OVERDUE</span>`:`<span class="badge badge-green">ON TIME</span>`}</td>
                <td><button class="btn btn-success btn-sm" onclick="returnBook(${h.resourceId})">Return</button></td>
              </tr>`;
            }).join('')}
          </tbody>
        </table>
      </div>
    `);
}

function renderHistory() {
  const u = getCurrentUser();
  setTopbarActions('');
  const hist = [...u.borrowHistory].reverse();

  setContent(hist.length===0
    ? `<div class="empty-state"><div class="es-icon">🕑</div><div class="es-text">No borrow history</div><div class="es-sub">Start borrowing books to see history here</div></div>`
    : `<div class="table-wrap"><table>
        <thead><tr><th>BOOK</th><th>BORROW DATE</th><th>DUE DATE</th><th>RETURN DATE</th><th>STATUS</th><th>FINE</th></tr></thead>
        <tbody>
          ${hist.map(h=>{
            const over = h.returned ? (h.returnDate > h.dueDate) : isOverdue(h.dueDate);
            let fine = '—';
            if (h.returned && h.returnDate > h.dueDate) {
              const r = db.resources.find(r=>r.id===h.resourceId);
              const days = daysDiff(h.dueDate, h.returnDate);
              const rate = r ? getFineRate(r.type) : 2;
              const mem = getMembership(u.membership);
              fine = 'Rs.'+(days*rate*(1-mem.fineDiscount)).toFixed(2);
            }
            return `<tr>
              <td class="primary">${h.title}</td>
              <td style="font-family:'DM Mono',monospace;font-size:11px">${fmtDate(h.borrowDate)}</td>
              <td style="font-family:'DM Mono',monospace;font-size:11px">${fmtDate(h.dueDate)}</td>
              <td style="font-family:'DM Mono',monospace;font-size:11px">${h.returned?fmtDate(h.returnDate):'—'}</td>
              <td>${h.returned?(over?`<span class="badge badge-red">LATE</span>`:`<span class="badge badge-green">ON TIME</span>`):(over?`<span class="badge badge-red">OVERDUE</span>`:`<span class="badge badge-blue">ACTIVE</span>`)}</td>
              <td style="color:var(--red);font-family:'DM Mono',monospace;font-size:11px">${fine}</td>
            </tr>`;
          }).join('')}
        </tbody>
      </table></div>`);
}

function renderProfile() {
  const u = getCurrentUser();
  setTopbarActions(`<button class="btn btn-primary" onclick="openEditProfile()">✏ Edit Profile</button>`);
  const mem = getMembership(u.membership);

  setContent(`
    <div class="profile-card">
      <div class="profile-header">
        <div class="profile-avatar">${u.firstName[0]}</div>
        <div class="profile-info">
          <h2>${u.firstName} ${u.lastName}</h2>
          <div class="profile-meta">${u.email} · ID #${u.id}</div>
          <div style="margin-top:8px"><span class="badge ${{Essential:'badge-blue',Extra:'badge-gold',Deluxe:'badge-purple'}[u.membership]}">${u.membership}</span>
          ${u.isLocked?`<span class="badge badge-red" style="margin-left:6px">🔒 LOCKED</span>`:''}</div>
        </div>
      </div>
      <div class="profile-stats">
        <div class="pstat"><div class="pstat-val">Rs.${u.balance.toFixed(0)}</div><div class="pstat-label">BALANCE</div></div>
        <div class="pstat"><div class="pstat-val">${u.loyaltyPoints}</div><div class="pstat-label">LOYALTY PTS</div></div>
        <div class="pstat"><div class="pstat-val">${u.borrowHistory.length}</div><div class="pstat-label">TOTAL BORROWS</div></div>
      </div>
    </div>
    <div class="profile-card">
      <div style="font-family:'Playfair Display',serif;font-size:16px;font-weight:700;margin-bottom:14px">Account Details</div>
      <div style="display:grid;grid-template-columns:1fr 1fr;gap:12px;font-size:13px">
        <div><div style="color:var(--text3);font-size:10px;font-family:'DM Mono',monospace;margin-bottom:3px">FIRST NAME</div><div>${u.firstName}</div></div>
        <div><div style="color:var(--text3);font-size:10px;font-family:'DM Mono',monospace;margin-bottom:3px">LAST NAME</div><div>${u.lastName}</div></div>
        <div><div style="color:var(--text3);font-size:10px;font-family:'DM Mono',monospace;margin-bottom:3px">EMAIL</div><div>${u.email}</div></div>
        <div><div style="color:var(--text3);font-size:10px;font-family:'DM Mono',monospace;margin-bottom:3px">MEMBERSHIP</div><div>${u.membership}</div></div>
        <div><div style="color:var(--text3);font-size:10px;font-family:'DM Mono',monospace;margin-bottom:3px">BORROW LIMIT</div><div>${mem.maxBorrow} books</div></div>
        <div><div style="color:var(--text3);font-size:10px;font-family:'DM Mono',monospace;margin-bottom:3px">FINE DISCOUNT</div><div>${mem.fineDiscount*100}%</div></div>
      </div>
    </div>
  `);
}

function openEditProfile() {
  const u = getCurrentUser();
  openModal('Edit Profile', `
    <div class="form-row">
      <div class="form-group"><label class="form-label">FIRST NAME</label><input class="form-control" id="ep-fn" value="${u.firstName}"></div>
      <div class="form-group"><label class="form-label">LAST NAME</label><input class="form-control" id="ep-ln" value="${u.lastName}"></div>
    </div>
    <div class="form-group"><label class="form-label">EMAIL ADDRESS</label><input class="form-control" id="ep-em" type="email" value="${u.email}"></div>
    <div class="form-group"><label class="form-label">NEW PASSWORD (leave blank to keep current)</label><input class="form-control" id="ep-pw" type="password" placeholder="New password"></div>
    <div class="form-actions">
      <button class="btn btn-secondary" onclick="closeModal()">Cancel</button>
      <button class="btn btn-primary" onclick="saveProfile()">Save Changes</button>
    </div>
  `);
}

function saveProfile() {
  const u = getCurrentUser();
  const fn = document.getElementById('ep-fn').value.trim();
  const ln = document.getElementById('ep-ln').value.trim();
  const em = document.getElementById('ep-em').value.trim();
  const pw = document.getElementById('ep-pw').value.trim();
  if (!fn||!ln||!em) { toast('All fields required.','error'); return; }
  u.firstName = fn; u.lastName = ln; u.email = em;
  if (pw) u.password = pw;
  saveData(); updateSidebar(); closeModal();
  toast('Profile updated!', 'success');
  navigate('profile');
}

function renderMembership() {
  const u = getCurrentUser();
  const tiers = ['Essential','Extra','Deluxe'];
  const perks = {
    Essential: ['Borrow up to 2 books','No fine discounts','Standard library access'],
    Extra: ['Borrow up to 5 books','25% discount on all fines','Priority reservation','Early access to new arrivals'],
    Deluxe: ['Borrow up to 10 books','50% discount on all fines','Free fine waiver (once/month)','Priority reservation & hold queue','Early access 7 days ahead','Loyalty points at 1.5x rate','Monthly book recommendations'],
  };
  const prices = { Essential:'Free', Extra:'Rs.10/month', Deluxe:'Rs.20/month' };
  const colors = { Essential:'badge-blue', Extra:'badge-gold', Deluxe:'badge-purple' };

  setTopbarActions('');
  setContent(`
    <div style="margin-bottom:20px;background:var(--surface);border:1px solid var(--border);border-radius:var(--radius);padding:18px 20px">
      <div style="display:flex;align-items:center;gap:12px">
        <div>
          <div style="font-size:11px;color:var(--text3);font-family:'DM Mono',monospace;margin-bottom:4px">CURRENT MEMBERSHIP</div>
          <div style="font-family:'Playfair Display',serif;font-size:22px;font-weight:700">${u.membership}</div>
        </div>
        <span class="badge ${colors[u.membership]}" style="font-size:12px;padding:5px 12px">${u.membership.toUpperCase()}</span>
      </div>
    </div>
    <div class="membership-grid">
      ${tiers.map(tier=>`
        <div class="mem-card ${u.membership===tier?'current':''}">
          <div class="mc-tier">TIER ${tiers.indexOf(tier)+1}</div>
          <div class="mc-name">${tier}</div>
          <div class="mc-price">${prices[tier]}</div>
          <div class="mc-perks">${perks[tier].map(p=>`• ${p}`).join('<br>')}</div>
          ${u.membership===tier
            ?`<div class="mc-badge"><span class="badge badge-green">CURRENT</span></div>`
            :`<button class="btn btn-primary btn-sm" style="margin-top:16px;width:100%;justify-content:center" onclick="changeMembership('${tier}')">Switch to ${tier}</button>`}
        </div>
      `).join('')}
    </div>
  `);
}

function changeMembership(tier) {
  const u = getCurrentUser();
  const costs = {Essential:0, Extra:10, Deluxe:20};
  const cost = costs[tier]||0;
  const mem = getMembership(tier);
  const currentMem = getMembership(u.membership);

  if (u.membership===tier) { toast(`You already have ${tier} membership.`,'info'); return; }
  const isDowngrade = ['Essential','Extra','Deluxe'].indexOf(tier) < ['Essential','Extra','Deluxe'].indexOf(u.membership);

  openModal(`Switch to ${tier} Membership`, `
    <div style="margin-bottom:16px">
      ${isDowngrade?`<div style="background:rgba(224,85,85,0.08);border:1px solid rgba(224,85,85,0.2);border-radius:8px;padding:12px;font-size:12px;color:var(--red);margin-bottom:14px">⚠ Downgrading will reduce your borrow limit from ${currentMem.maxBorrow} to ${mem.maxBorrow} books.</div>`:''}
      <div style="font-size:13px;color:var(--text2);margin-bottom:8px">Switching from <strong>${u.membership}</strong> to <strong>${tier}</strong>.</div>
      <div style="font-size:13px;color:var(--text2)">Cost: <strong style="color:var(--accent)">${cost===0?'Free':'Rs.'+cost}</strong></div>
      ${cost>0?`<div style="font-size:11px;color:var(--text3);margin-top:6px">Your balance: Rs.${u.balance.toFixed(2)}</div>`:''}
    </div>
    <div class="form-actions">
      <button class="btn btn-secondary" onclick="closeModal()">Cancel</button>
      <button class="btn btn-primary" onclick="confirmMembership('${tier}',${cost})">Confirm</button>
    </div>
  `);
}

function confirmMembership(tier, cost) {
  const u = getCurrentUser();
  if (cost > 0 && u.balance < cost) { toast(`Insufficient balance. Need Rs.${cost}.`,'error'); closeModal(); return; }
  if (cost > 0) u.balance -= cost;
  u.membership = tier;
  saveData(); closeModal(); updateSidebar();
  toast(`Membership changed to ${tier}!`, 'success');
  navigate('membership');
}

function renderLoyalty() {
  const u = getCurrentUser();
  setTopbarActions('');
  setContent(`
    <div class="stats-grid" style="grid-template-columns:repeat(3,1fr)">
      <div class="stat-card">
        <div class="sc-label">LOYALTY POINTS</div>
        <div class="sc-value">${u.loyaltyPoints}</div>
        <div class="sc-sub">Total earned</div>
        <div class="sc-icon">⭐</div>
      </div>
      <div class="stat-card">
        <div class="sc-label">EQUIVALENT VALUE</div>
        <div class="sc-value" style="font-size:24px">Rs.${((u.loyaltyPoints/100)*50).toFixed(0)}</div>
        <div class="sc-sub">100 pts = Rs.50</div>
        <div class="sc-icon">💎</div>
      </div>
      <div class="stat-card">
        <div class="sc-label">TOTAL BORROWS</div>
        <div class="sc-value">${u.borrowHistory.length}</div>
        <div class="sc-sub">5 pts per borrow, 10 per on-time return</div>
        <div class="sc-icon">📚</div>
      </div>
    </div>
    <div style="display:grid;grid-template-columns:1fr 1fr;gap:20px">
      <div>
        <div class="section-header"><div class="section-title">Redeem Points</div></div>
        <div style="display:flex;flex-direction:column;gap:12px">
          ${[
            {pts:100, label:'Fine-Free Pass', desc:'Next overdue fine waived', icon:'🎫'},
            {pts:200, label:'Rs.100 Balance Credit', desc:'Added to your wallet', icon:'💰'},
            {pts:500, label:'Rs.250 Credit + Free Rental', desc:'Big saver bundle', icon:'🎁'},
            {pts:1000, label:'Membership Upgrade', desc:'Upgrade to next tier free', icon:'👑'},
          ].map(o=>`
            <div class="history-item" style="${u.loyaltyPoints<o.pts?'opacity:0.5':''}">
              <div class="hi-icon borrowed" style="font-size:20px">${o.icon}</div>
              <div class="hi-info">
                <div class="hi-title">${o.label}</div>
                <div class="hi-meta">${o.desc} · <strong>${o.pts} points</strong></div>
              </div>
              <button class="btn btn-primary btn-sm" ${u.loyaltyPoints<o.pts?'disabled style="opacity:0.4;cursor:not-allowed"':''} onclick="redeemPoints(${o.pts},'${o.label}')">Redeem</button>
            </div>
          `).join('')}
        </div>
      </div>
      <div>
        <div class="section-header"><div class="section-title">How to Earn</div></div>
        <div class="profile-card">
          <div style="display:flex;flex-direction:column;gap:10px;font-size:13px">
            ${[
              ['📖','Borrow a book','5 points'],
              ['✅','Return on time','10 points'],
              ['⭐','Rate a book','2 points'],
              ['🔄','Monthly bonus','50 points'],
            ].map(([icon,label,pts])=>`
              <div style="display:flex;align-items:center;justify-content:space-between;padding:8px 0;border-bottom:1px solid var(--border)">
                <div style="display:flex;align-items:center;gap:10px"><span>${icon}</span><span>${label}</span></div>
                <span style="color:var(--accent);font-family:'DM Mono',monospace;font-weight:600">+${pts}</span>
              </div>
            `).join('')}
          </div>
        </div>
      </div>
    </div>
  `);
}

function redeemPoints(pts, label) {
  const u = getCurrentUser();
  if (u.loyaltyPoints < pts) { toast('Not enough points.','error'); return; }
  u.loyaltyPoints -= pts;
  if (pts===200) { u.balance += 100; toast(`Redeemed! Rs.100 added to wallet.`,'success'); }
  else if (pts===500) { u.balance += 250; toast(`Redeemed! Rs.250 added + free rental.`,'success'); }
  else if (pts===1000) {
    if (u.membership==='Essential') { u.membership='Extra'; toast(`Upgraded to Extra membership!`,'success'); }
    else if (u.membership==='Extra') { u.membership='Deluxe'; toast(`Upgraded to Deluxe membership!`,'success'); }
    else { u.loyaltyPoints += pts; toast('Already at Deluxe tier.','info'); return; }
    updateSidebar();
  } else { toast(`"${label}" redeemed!`,'success'); }
  saveData();
  navigate('loyalty');
}

function renderWallet() {
  const u = getCurrentUser();
  setTopbarActions(`<button class="btn btn-primary" onclick="openRecharge()">+ Add Funds</button>`);
  setContent(`
    <div class="stats-grid" style="grid-template-columns:repeat(2,1fr);max-width:500px">
      <div class="stat-card">
        <div class="sc-label">CURRENT BALANCE</div>
        <div class="sc-value" style="color:${u.balance<0?'var(--red)':'var(--accent)'}">${u.balance<0?'-':''}Rs.${Math.abs(u.balance).toFixed(2)}</div>
        <div class="sc-sub">${u.balance<0?'<span style="color:var(--red)">Debt — please recharge</span>':'Available to use'}</div>
      </div>
      <div class="stat-card">
        <div class="sc-label">LOYALTY BALANCE</div>
        <div class="sc-value" style="font-size:22px">Rs.${((u.loyaltyPoints/100)*50).toFixed(0)}</div>
        <div class="sc-sub">${u.loyaltyPoints} points redeemable</div>
      </div>
    </div>
    <div style="margin-top:20px">
      <div class="section-header"><div class="section-title">Quick Actions</div></div>
      <div style="display:flex;gap:12px;flex-wrap:wrap">
        ${[50,100,200,500].map(a=>`<button class="btn btn-secondary" onclick="quickRecharge(${a})">+ Rs.${a}</button>`).join('')}
        <button class="btn btn-primary" onclick="openRecharge()">Custom Amount</button>
      </div>
    </div>
    ${u.balance<0?`<div class="fine-bar" style="margin-top:20px"><div class="fine-label">⚠ OUTSTANDING DEBT: Rs.${Math.abs(u.balance).toFixed(2)}</div><button class="btn btn-danger btn-sm" onclick="openRecharge()">Pay Off</button></div>`:''}
  `);
}

function quickRecharge(amount) {
  const u = getCurrentUser();
  u.balance += amount;
  saveData();
  toast(`Rs.${amount} added to your wallet!`,'success');
  navigate('wallet');
}

function openRecharge() {
  openModal('Add Funds to Wallet', `
    <div class="form-group"><label class="form-label">AMOUNT (Rs.)</label><input class="form-control" id="rch-amt" type="number" min="1" placeholder="Enter amount" value="100"></div>
    <div class="form-actions">
      <button class="btn btn-secondary" onclick="closeModal()">Cancel</button>
      <button class="btn btn-primary" onclick="doRecharge()">Add Funds</button>
    </div>
  `);
}

function doRecharge() {
  const amt = parseFloat(document.getElementById('rch-amt').value);
  if (!amt || amt<=0) { toast('Enter a valid amount.','error'); return; }
  const u = getCurrentUser();
  u.balance += amt;
  saveData(); closeModal();
  toast(`Rs.${amt} added to wallet!`,'success');
  navigate('wallet');
}

// ═══════════════════════════════════════════════════════
//  PAGES — ADMIN
// ═══════════════════════════════════════════════════════

function renderAdminDashboard() {
  const totalUsers = db.users.length;
  const totalBooks = db.resources.length;
  const borrowed = db.resources.filter(r=>!r.available).length;
  const available = db.resources.filter(r=>r.available).length;
  const lockedUsers = db.users.filter(u=>u.isLocked).length;
  const overdueCount = db.users.reduce((acc,u)=>acc+u.borrowHistory.filter(h=>!h.returned&&isOverdue(h.dueDate)).length,0);

  setTopbarActions('');
  setContent(`
    <div class="stats-grid">
      <div class="stat-card">
        <div class="sc-label">TOTAL USERS</div>
        <div class="sc-value">${totalUsers}</div>
        <div class="sc-sub">${lockedUsers} locked</div>
        <div class="sc-icon">👥</div>
      </div>
      <div class="stat-card">
        <div class="sc-label">TOTAL BOOKS</div>
        <div class="sc-value">${totalBooks}</div>
        <div class="sc-sub">${available} available</div>
        <div class="sc-icon">📚</div>
      </div>
      <div class="stat-card">
        <div class="sc-label">CURRENTLY BORROWED</div>
        <div class="sc-value">${borrowed}</div>
        <div class="sc-sub">of ${totalBooks} total</div>
        <div class="sc-icon">📖</div>
      </div>
      <div class="stat-card">
        <div class="sc-label">OVERDUE BOOKS</div>
        <div class="sc-value" style="color:${overdueCount>0?'var(--red)':'var(--green)'}">${overdueCount}</div>
        <div class="sc-sub">${overdueCount>0?'Needs attention':'All on time'}</div>
        <div class="sc-icon">⏰</div>
      </div>
    </div>

    <div style="display:grid;grid-template-columns:1fr 1fr;gap:20px">
      <div>
        <div class="section-header"><div class="section-title">User Overview</div><button class="btn btn-secondary btn-sm" onclick="navigate('manage-users')">Manage</button></div>
        <div class="table-wrap">
          <table>
            <thead><tr><th>NAME</th><th>MEMBERSHIP</th><th>STATUS</th><th>BALANCE</th></tr></thead>
            <tbody>
              ${db.users.map(u=>`
                <tr>
                  <td class="primary">${u.firstName} ${u.lastName}</td>
                  <td><span class="badge ${{Essential:'badge-blue',Extra:'badge-gold',Deluxe:'badge-purple'}[u.membership]}">${u.membership}</span></td>
                  <td>${u.isLocked?`<span class="badge badge-red">LOCKED</span>`:`<span class="badge badge-green">ACTIVE</span>`}</td>
                  <td style="font-family:'DM Mono',monospace;font-size:11px;color:${u.balance<0?'var(--red)':'var(--text2)'}">Rs.${u.balance.toFixed(0)}</td>
                </tr>
              `).join('')}
            </tbody>
          </table>
        </div>
      </div>
      <div>
        <div class="section-header"><div class="section-title">Book Overview</div><button class="btn btn-secondary btn-sm" onclick="navigate('manage-books')">Manage</button></div>
        <div class="table-wrap">
          <table>
            <thead><tr><th>TITLE</th><th>TYPE</th><th>STATUS</th><th>BORROWS</th></tr></thead>
            <tbody>
              ${db.resources.map(r=>`
                <tr>
                  <td class="primary" style="max-width:150px;overflow:hidden;text-overflow:ellipsis;white-space:nowrap">${r.title}</td>
                  <td><span class="badge ${{PrimePick:'badge-gold',ClassicShelf:'badge-purple',BudgetPick:'badge-green'}[r.type]||'badge-blue'}" style="font-size:9px">${r.type}</span></td>
                  <td>${r.available?`<span class="badge badge-green">AVAIL</span>`:`<span class="badge badge-red">OUT</span>`}</td>
                  <td style="font-family:'DM Mono',monospace;font-size:11px">${r.borrowCount}</td>
                </tr>
              `).join('')}
            </tbody>
          </table>
        </div>
      </div>
    </div>
  `);
}

function renderManageBooks() {
  setTopbarActions(`<button class="btn btn-primary" onclick="openAddBook()">+ Add Book</button>`);

  const typeLabel = {PrimePick:'Prime Pick',ClassicShelf:'Classic Shelf',BudgetPick:'Budget Pick'};
  const typeColor = {PrimePick:'badge-gold',ClassicShelf:'badge-purple',BudgetPick:'badge-green'};

  setContent(`
    <div class="table-wrap">
      <table>
        <thead><tr><th>ID</th><th>TITLE</th><th>AUTHOR</th><th>CATEGORY</th><th>TYPE</th><th>STATUS</th><th>RATING</th><th>BORROWS</th><th>ACTIONS</th></tr></thead>
        <tbody>
          ${db.resources.map(r=>`
            <tr>
              <td style="font-family:'DM Mono',monospace;font-size:11px;color:var(--text3)">#${r.id}</td>
              <td class="primary">${r.title}</td>
              <td>${r.author}</td>
              <td><span class="badge badge-blue">${r.category}</span></td>
              <td><span class="badge ${typeColor[r.type]||'badge-blue'}">${typeLabel[r.type]||r.type}</span></td>
              <td>${r.available?`<span class="badge badge-green">AVAILABLE</span>`:`<span class="badge badge-red">BORROWED</span>`}</td>
              <td style="font-family:'DM Mono',monospace;font-size:11px">★ ${r.rating>0?r.rating.toFixed(1):'—'}</td>
              <td style="font-family:'DM Mono',monospace;font-size:11px">${r.borrowCount}</td>
              <td>
                <div style="display:flex;gap:6px">
                  ${!r.available?`<button class="btn btn-success btn-sm" onclick="adminMarkAvailable(${r.id})">Mark Available</button>`:''}
                  <button class="btn btn-danger btn-sm" onclick="adminDeleteBook(${r.id})">Delete</button>
                </div>
              </td>
            </tr>
          `).join('')}
        </tbody>
      </table>
    </div>
  `);
}

function openAddBook() {
  openModal('Add New Book', `
    <div class="form-group"><label class="form-label">TITLE</label><input class="form-control" id="nb-title" placeholder="Book title"></div>
    <div class="form-group"><label class="form-label">AUTHOR</label><input class="form-control" id="nb-author" placeholder="Author name"></div>
    <div class="form-row">
      <div class="form-group">
        <label class="form-label">CATEGORY</label>
        <select class="form-control" id="nb-cat">
          <option value="Fiction">Fiction</option>
          <option value="Science">Science</option>
          <option value="History">History</option>
          <option value="Technology">Technology</option>
          <option value="Other">Other</option>
        </select>
      </div>
      <div class="form-group">
        <label class="form-label">TYPE</label>
        <select class="form-control" id="nb-type">
          <option value="PrimePick">Prime Pick (Fine: Rs.5/day)</option>
          <option value="ClassicShelf">Classic Shelf (Fine: Rs.3/day)</option>
          <option value="BudgetPick">Budget Pick (Fine: Rs.2/day)</option>
        </select>
      </div>
    </div>
    <div class="form-actions">
      <button class="btn btn-secondary" onclick="closeModal()">Cancel</button>
      <button class="btn btn-primary" onclick="adminAddBook()">Add Book</button>
    </div>
  `);
}

function adminAddBook() {
  const title  = document.getElementById('nb-title').value.trim();
  const author = document.getElementById('nb-author').value.trim();
  const cat    = document.getElementById('nb-cat').value;
  const type   = document.getElementById('nb-type').value;
  if (!title||!author) { toast('Title and author required.','error'); return; }
  const nb = { type, title, author, category:cat, available:true, rating:0, borrowCount:0, isNewArrival:true };
  addResources(nb);
  closeModal();
  toast(`Book "${title}" added!`,'success');
  navigate('manage-books');
}

function adminMarkAvailable(id) {
  const r = getResource(id);
  r.available = true;
  saveData(); toast(`"${r.title}" marked as available.`,'success'); navigate('manage-books');
}

function adminDeleteBook(id) {
  const r = getResource(id);
  if (!r.available) { toast('Cannot delete a borrowed book.','error'); return; }
  openModal('Confirm Delete', `
    <div style="font-size:13px;color:var(--text2);margin-bottom:20px">Delete "<strong>${r.title}</strong>"? This cannot be undone.</div>
    <div class="form-actions">
      <button class="btn btn-secondary" onclick="closeModal()">Cancel</button>
      <button class="btn btn-danger" onclick="confirmDeleteBook(${id})">Delete</button>
    </div>
  `);
}

function confirmDeleteBook(id) {
  if (!removeResources(id)) { toast('Cannot delete a borrowed book.','error'); return; }
  closeModal(); toast('Book deleted.','info'); navigate('manage-books');
}

function renderManageUsers() {
  setTopbarActions('');
  setContent(`
    <div class="table-wrap">
      <table>
        <thead><tr><th>ID</th><th>NAME</th><th>EMAIL</th><th>MEMBERSHIP</th><th>BALANCE</th><th>POINTS</th><th>BORROWS</th><th>STATUS</th><th>ACTIONS</th></tr></thead>
        <tbody>
          ${db.users.map(u=>`
            <tr>
              <td style="font-family:'DM Mono',monospace;font-size:11px;color:var(--text3)">#${u.id}</td>
              <td class="primary">${u.firstName} ${u.lastName}</td>
              <td style="font-size:11px;color:var(--text3)">${u.email}</td>
              <td><span class="badge ${{Essential:'badge-blue',Extra:'badge-gold',Deluxe:'badge-purple'}[u.membership]}">${u.membership}</span></td>
              <td style="font-family:'DM Mono',monospace;font-size:11px;color:${u.balance<0?'var(--red)':'var(--text2)'}">Rs.${u.balance.toFixed(0)}</td>
              <td style="font-family:'DM Mono',monospace;font-size:11px">${u.loyaltyPoints}</td>
              <td style="font-family:'DM Mono',monospace;font-size:11px">${u.borrowHistory.length}</td>
              <td>${u.isLocked?`<span class="badge badge-red">LOCKED</span>`:`<span class="badge badge-green">ACTIVE</span>`}</td>
              <td>
                <div style="display:flex;gap:6px;flex-wrap:wrap">
                  ${u.isLocked
                    ?`<button class="btn btn-success btn-sm" onclick="adminUnlockUser(${u.id})">Unlock</button>`
                    :`<button class="btn btn-danger btn-sm" onclick="adminLockUser(${u.id})">Lock</button>`}
                  <button class="btn btn-secondary btn-sm" onclick="openAssignMembership(${u.id})">Membership</button>
                  <button class="btn btn-secondary btn-sm" onclick="openAdminRecharge(${u.id})">Recharge</button>
                </div>
              </td>
            </tr>
          `).join('')}
        </tbody>
      </table>
    </div>
  `);
}

function adminLockUser(id) {
  if (!lockUser(id)) { toast('Unable to lock user.','error'); return; }
  const u = getUser(id);
  toast(`${u.firstName} ${u.lastName} locked.`,'info'); navigate('manage-users');
}
function adminUnlockUser(id) {
  if (!unlockUser(id)) { toast('Unable to unlock user.','error'); return; }
  const u = getUser(id);
  toast(`${u.firstName} ${u.lastName} unlocked.`,'success'); navigate('manage-users');
}

function openAssignMembership(id) {
  const u = getUser(id);
  openModal(`Assign Membership — ${u.firstName} ${u.lastName}`, `
    <div style="margin-bottom:14px;font-size:13px;color:var(--text2)">Current: <strong>${u.membership}</strong></div>
    <div class="form-group">
      <label class="form-label">SELECT TIER</label>
      <select class="form-control" id="am-tier">
        <option value="Essential">Essential (Free)</option>
        <option value="Extra">Extra (Rs.10/month)</option>
        <option value="Deluxe">Deluxe (Rs.20/month)</option>
      </select>
    </div>
    <div class="form-actions">
      <button class="btn btn-secondary" onclick="closeModal()">Cancel</button>
      <button class="btn btn-primary" onclick="adminAssignMembership(${id})">Assign</button>
    </div>
  `);
  document.getElementById('am-tier').value = u.membership;
}

function adminAssignMembership(id) {
  const tier = document.getElementById('am-tier').value;
  if (!assignMembershipTier(id, tier)) { toast('Unable to update membership.','error'); return; }
  const u = getUser(id);
  closeModal(); toast(`Membership updated to ${u.membership}.`,'success'); navigate('manage-users');
}

function openAdminRecharge(id) {
  const u = getUser(id);
  openModal(`Recharge — ${u.firstName} ${u.lastName}`, `
    <div style="margin-bottom:14px;font-size:13px;color:var(--text2)">Current balance: <strong style="color:var(--accent)">Rs.${u.balance.toFixed(2)}</strong></div>
    <div class="form-group"><label class="form-label">AMOUNT (Rs.)</label><input class="form-control" id="ar-amt" type="number" min="1" placeholder="Enter amount" value="100"></div>
    <div class="form-actions">
      <button class="btn btn-secondary" onclick="closeModal()">Cancel</button>
      <button class="btn btn-primary" onclick="adminDoRecharge(${id})">Add Funds</button>
    </div>
  `);
}

function adminDoRecharge(id) {
  const amt = parseFloat(document.getElementById('ar-amt').value);
  if (!amt||amt<=0) { toast('Invalid amount.','error'); return; }
  if (!rechargebalanceUser(id, amt)) { toast('Unable to add funds.','error'); return; }
  const u = getUser(id);
  closeModal(); toast(`Rs.${amt} added to ${u.firstName}'s wallet.`,'success'); navigate('manage-users');
}

function renderReports() {
  setTopbarActions('');
  const overdueBooks = generateOverdueResourcesReport();
  const totalFines = generateFineReport().total;
  const issuedBooks = generateIssuedResourcesReport();
  const topBorrowed = filterByMostBorrowed().slice(0,5);

  setContent(`
    <div class="stats-grid">
      <div class="stat-card"><div class="sc-label">OVERDUE BOOKS</div><div class="sc-value" style="color:${overdueBooks.length>0?'var(--red)':'var(--green)'}">${overdueBooks.length}</div><div class="sc-sub">Needs attention</div><div class="sc-icon">⏰</div></div>
      <div class="stat-card"><div class="sc-label">TOTAL FINES COLLECTED</div><div class="sc-value" style="font-size:22px">Rs.${totalFines.toFixed(0)}</div><div class="sc-sub">All time</div><div class="sc-icon">💸</div></div>
      <div class="stat-card"><div class="sc-label">BOOKS ISSUED</div><div class="sc-value">${issuedBooks.length}</div><div class="sc-sub">Currently out</div><div class="sc-icon">📤</div></div>
      <div class="stat-card"><div class="sc-label">TOTAL BORROWS</div><div class="sc-value">${db.resources.reduce((a,r)=>a+r.borrowCount,0)}</div><div class="sc-sub">All time</div><div class="sc-icon">📊</div></div>
    </div>

    <div style="display:grid;grid-template-columns:1fr 1fr;gap:20px;margin-bottom:20px">
      <div class="report-section">
        <div class="report-header"><span>OVERDUE BOOKS REPORT</span><span style="color:var(--red)">${overdueBooks.length} overdue</span></div>
        <div class="report-body">
          ${overdueBooks.length===0?`<div class="empty-state" style="padding:20px"><div class="es-icon" style="font-size:24px">✅</div><div class="es-text">No overdue books</div></div>`
          :overdueBooks.slice(0,8).map(({user:u,record:h})=>`
            <div style="display:flex;justify-content:space-between;align-items:center;padding:8px 0;border-bottom:1px solid var(--border);font-size:12px">
              <div>
                <div style="font-weight:500">${h.title}</div>
                <div style="color:var(--text3);font-size:10px;font-family:'DM Mono',monospace">${u.firstName} ${u.lastName} · Due ${fmtDate(h.dueDate)}</div>
              </div>
              <span class="badge badge-red">${daysDiff(h.dueDate,today())}d late</span>
            </div>
          `).join('')}
        </div>
      </div>
      <div class="report-section">
        <div class="report-header"><span>MOST BORROWED BOOKS</span></div>
        <div class="report-body">
          ${topBorrowed.map((r,i)=>`
            <div style="display:flex;align-items:center;gap:12px;padding:8px 0;border-bottom:1px solid var(--border);font-size:12px">
              <div style="width:24px;height:24px;border-radius:50%;background:var(--accent-dim);color:var(--accent);display:flex;align-items:center;justify-content:center;font-size:11px;font-weight:700;flex-shrink:0">#${i+1}</div>
              <div style="flex:1">
                <div style="font-weight:500">${r.title}</div>
                <div style="color:var(--text3);font-size:10px">${r.author}</div>
              </div>
              <div style="text-align:right">
                <div style="font-family:'DM Mono',monospace;color:var(--accent)">${r.borrowCount} borrows</div>
                <div style="font-size:10px;color:var(--text3)">★ ${r.rating>0?r.rating.toFixed(1):'—'}</div>
              </div>
            </div>
          `).join('')}
        </div>
      </div>
    </div>

    <div class="report-section">
      <div class="report-header"><span>CUSTOMER REPORT — ALL USERS</span></div>
      <div class="report-body" style="padding:0">
        <table style="width:100%;border-collapse:collapse">
          <thead><tr style="border-bottom:1px solid var(--border)">
            ${['ID','Name','Email','Membership','Balance','Loyalty Pts','Active Borrows','Status'].map(h=>`<th style="padding:10px 16px;text-align:left;font-size:10px;color:var(--text3);font-family:'DM Mono',monospace;letter-spacing:1.5px;background:var(--surface2)">${h}</th>`).join('')}
          </tr></thead>
          <tbody>
            ${db.users.map(u=>`
              <tr style="border-bottom:1px solid var(--border)">
                <td style="padding:10px 16px;font-size:11px;color:var(--text3);font-family:'DM Mono',monospace">#${u.id}</td>
                <td style="padding:10px 16px;font-size:13px;font-weight:500">${u.firstName} ${u.lastName}</td>
                <td style="padding:10px 16px;font-size:11px;color:var(--text3)">${u.email}</td>
                <td style="padding:10px 16px"><span class="badge ${{Essential:'badge-blue',Extra:'badge-gold',Deluxe:'badge-purple'}[u.membership]}">${u.membership}</span></td>
                <td style="padding:10px 16px;font-size:11px;font-family:'DM Mono',monospace;color:${u.balance<0?'var(--red)':'var(--text2)'}">Rs.${u.balance.toFixed(2)}</td>
                <td style="padding:10px 16px;font-size:11px;font-family:'DM Mono',monospace">${u.loyaltyPoints}</td>
                <td style="padding:10px 16px;font-size:11px;font-family:'DM Mono',monospace">${u.currentlyBorrowed.length}</td>
                <td style="padding:10px 16px">${u.isLocked?`<span class="badge badge-red">LOCKED</span>`:`<span class="badge badge-green">ACTIVE</span>`}</td>
              </tr>
            `).join('')}
          </tbody>
        </table>
      </div>
    </div>
  `);
}

// ═══════════════════════════════════════════════════════
//  LIBRARYSYSTEM / USER / ADMIN API WRAPPERS
// ═══════════════════════════════════════════════════════

function getResourceByID(resourceID) { return getResource(resourceID); }
function addUser(user) { db.users.push(user); saveData(); return user; }
function addAdmin(admin) { db.admins.push(admin); saveData(); return admin; }
function registerUser(firstName, lastName, email, password, initialBalance = 0.0) {
  const user = {
    id: db.nextUserId++,
    firstName,
    lastName,
    email,
    password,
    balance: initialBalance,
    loyaltyPoints: 0,
    membership: 'Essential',
    isLocked: false,
    borrowHistory: [],
    currentlyBorrowed: [],
  };
  db.users.push(user);
  saveData();
  return user;
}
function authenticate(email, password) {
  return db.users.find(u => u.email === email && u.password === password) || db.admins.find(a => a.email === email && a.password === password) || null;
}
function logout() { doLogout(); }

function showAllUsers() { return db.users; }
function showAllAdmins() { return db.admins; }

function searchResource(keyword) {
  const q = String(keyword || '').toLowerCase();
  return db.resources.filter(r =>
    r.title.toLowerCase().includes(q) ||
    r.author.toLowerCase().includes(q) ||
    r.category.toLowerCase().includes(q) ||
    r.type.toLowerCase().includes(q)
  );
}
function filterResources(category) {
  if (!category || category === 'All') return db.resources;
  return db.resources.filter(r => r.category === category);
}
function filterByAvailability() { return db.resources.filter(r => r.available); }
function filterByNewArrivals() { return db.resources.filter(r => r.isNewArrival); }
function filterByMostBorrowed() { return [...db.resources].sort((a, b) => b.borrowCount - a.borrowCount); }
function filterByRating() { return [...db.resources].sort((a, b) => b.rating - a.rating); }
function filterByUserPreference(user) {
  if (!user) return filterByRating();
  const favourites = user.favouriteCategories || [];
  if (favourites.length) return db.resources.filter(r => favourites.includes(r.category));
  const lastCategory = [...user.borrowHistory].reverse().find(h => h.category);
  if (lastCategory) return db.resources.filter(r => r.category === lastCategory.category);
  return filterByRating();
}

function borrowResource(resourceID, date = today()) { return borrowBook(resourceID); }
function returnResource(resourceID, date = today()) { return returnBook(resourceID); }
function changeUserMembershipTier(userID, tier, confirm = true) {
  const u = getUser(userID);
  if (!u) return false;
  if (confirm) {
    u.membership = tier;
    saveData();
    return true;
  }
  u.membership = tier;
  saveData();
  return true;
}
function getLockStatus(userID) { const u = getUser(userID); return u ? u.isLocked : false; }
function deductFromBalance(userID, amount) {
  const u = getUser(userID);
  if (!u || u.balance < amount) return false;
  u.balance -= amount;
  saveData();
  return true;
}
function rechargebalance(amount) { const u = getCurrentUser(); if (!u) return false; u.balance += amount; saveData(); return true; }
function earnpoints(points) { const u = getCurrentUser(); if (!u) return false; u.loyaltyPoints += points; saveData(); return true; }
function setMembership(tier) { const u = getCurrentUser(); if (!u) return false; u.membership = tier; saveData(); return true; }
function lockUser(userID) { const u = getUser(userID); if (!u) return false; u.isLocked = true; saveData(); return true; }
function unlockUser(userID) { const u = getUser(userID); if (!u) return false; u.isLocked = false; saveData(); return true; }
function viewhistory() { const u = getCurrentUser(); return u ? u.borrowHistory : []; }
function updateprofile(firstName, lastName, email, password) {
  const u = getCurrentUser();
  if (!u) return false;
  if (firstName) u.firstName = firstName;
  if (lastName) u.lastName = lastName;
  if (email) u.email = email;
  if (password) u.password = password;
  saveData();
  return true;
}
function displayInfo(id) {
  const p = id ? getUser(id) || getAdmin(id) : (session.type === 'admin' ? getCurrentAdmin() : getCurrentUser());
  return p || null;
}
function getID(user) { return user ? user.id : null; }
function getEmail(user) { return user ? user.email : ''; }
function getFullName(user) { return user ? `${user.firstName} ${user.lastName}` : ''; }
function getPassword(user) { return user ? user.password : ''; }
function getAccountBalance(user) { return user ? user.balance : 0; }
function getPreferredCategory(user) { return user ? (user.favouriteCategories && user.favouriteCategories[0]) || '' : ''; }
function getLoyaltyPoints(user) { return user ? user.loyaltyPoints : 0; }

function redeemPointsForDiscount(pointsToRedeem) {
  const u = getCurrentUser();
  if (!u || u.loyaltyPoints < pointsToRedeem) return false;
  u.loyaltyPoints -= pointsToRedeem;
  const discount = Math.floor(pointsToRedeem / 100) * 50;
  u.balance += discount;
  saveData();
  return true;
}
function redeemPointsForFineFreePass() {
  const u = getCurrentUser();
  if (!u || u.loyaltyPoints < 500) return false;
  u.loyaltyPoints -= 500;
  u.fineFreePass = true;
  saveData();
  return true;
}
function displayLoyaltySummary() {
  const u = getCurrentUser();
  if (!u) return 'No user signed in.';
  return `Loyalty points: ${u.loyaltyPoints}. Balance: Rs.${u.balance.toFixed(2)}. Membership: ${u.membership}.`;
}
function checkAndUpgradeMembership() {
  const u = getCurrentUser();
  if (!u) return false;
  if (u.loyaltyPoints >= 1000 && u.membership === 'Essential') { u.membership = 'Extra'; saveData(); return true; }
  if (u.loyaltyPoints >= 2000 && u.membership === 'Extra') { u.membership = 'Deluxe'; saveData(); return true; }
  return false;
}
function displayMembershipDetails() {
  const u = getCurrentUser();
  if (!u) return null;
  return {
    membership: u.membership,
    maxBorrow: getMembership(u.membership).maxBorrow,
    fineDiscount: getMembership(u.membership).fineDiscount,
  };
}
function getMembershipChangeNotice(tier) {
  const u = getCurrentUser();
  if (!u) return '';
  return `Switching from ${u.membership} to ${tier} will ${tier === u.membership ? 'keep your current benefits' : 'update your borrow limit and fine discount.'}`;
}
function showMembershipOptions() { return MEMBERSHIPS; }

function addResources(resource) {
  resource.id = db.nextResourceId++;
  db.resources.push(resource);
  saveData();
  return resource;
}
function removeResources(resourceID) {
  const r = getResource(resourceID);
  if (!r || !r.available) return false;
  db.resources = db.resources.filter(rr => rr.id !== resourceID);
  saveData();
  return true;
}
function updateResources(resourceID, updates) {
  const r = getResource(resourceID);
  if (!r) return false;
  Object.assign(r, updates);
  saveData();
  return true;
}
function approveDigitalUpload(resourceID) {
  const r = getResource(resourceID);
  if (!r) return false;
  r.available = true;
  r.isNewArrival = true;
  saveData();
  return true;
}
function assignCardType(userID, cardType) {
  const u = getUser(userID);
  if (!u) return false;
  u.cardType = cardType;
  saveData();
  return true;
}
function assignMembershipTier(userID, tier, system) { return changeUserMembershipTier(userID, tier, true); }
function generateCustomerReport() {
  return showAllUsers().map(u => ({
    id: u.id,
    name: `${u.firstName} ${u.lastName}`,
    email: u.email,
    membership: u.membership,
    balance: u.balance,
    loyaltyPoints: u.loyaltyPoints,
    activeBorrows: u.currentlyBorrowed.length,
    status: u.isLocked ? 'Locked' : 'Active',
  }));
}
function generateIssuedResourcesReport() { return db.resources.filter(r => !r.available); }
function generateOverdueResourcesReport() {
  const overdue = [];
  db.users.forEach(u => {
    u.borrowHistory.filter(h => !h.returned && isOverdue(h.dueDate)).forEach(h => {
      overdue.push({
        user: `${u.firstName} ${u.lastName}`,
        resource: getResource(h.resourceId),
        dueDate: h.dueDate,
        daysLate: daysDiff(h.dueDate, today()),
      });
    });
  });
  return overdue;
}
function generateFineReport() {
  const details = [];
  let total = 0;
  db.users.forEach(u => {
    u.borrowHistory.filter(h => h.returned && h.returnDate && isOverdue(h.dueDate, h.returnDate)).forEach(h => {
      const r = getResource(h.resourceId);
      const days = daysDiff(h.dueDate, h.returnDate);
      const rate = r ? getFineRate(r.type) : 2;
      const mem = getMembership(u.membership);
      const fine = days * rate * (1 - mem.fineDiscount);
      total += fine;
      details.push({ user: `${u.firstName} ${u.lastName}`, title: h.title, daysLate: days, fine });
    });
  });
  return { total, details };
}

// ═══════════════════════════════════════════════════════
//  KEYBOARD SHORTCUT
// ═══════════════════════════════════════════════════════
document.addEventListener('keydown', e => {
  if (e.key==='Escape') closeModal();
});
</script>
</body>
</html>